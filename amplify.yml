version: 1
env:
  variables:
    environment: dev
    PORT: 3000
    SERVER_PORT: 4001
    MONGO_URI: mongodb+srv://root:gmQ3kZT9aKJBQF7W@mernapp.9jdlshy.mongodb.net/?retryWrites=true&w=majority
backend:
  phases:
    build:
      commands:
        - npm ci
        - npm run server
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'

